version: 2

models:
  - name: patients
    columns:
      - name: patient_id
        tests:
          - unique
          - not_null

  - name: med_admins
    columns:
      - name: patient_id
        tests:
          - unique
          - not_null
          
  
  